@model IEnumerable<MonthlyClaimSystem_PartTwo.Models.Coordinator>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Lecturer)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Lecturer.LecturerId)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CoordinatorId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CoordinatorId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CoordinatorId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
